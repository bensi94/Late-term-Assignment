defaultTasks 'frontend:clean',
        'frontend:yarn',
        'frontend:yarn_run_build',
        'frontend:webjar',
        'backend:clean',
        'backend:build',
        'backend:installDist'

// stage.mustRunAfter(clean)
//
// clean << {
//     project.file('backend/build/libs/Late-term-Assignment-0.0.1-SNAPSHOT.jar').delete()
// }
